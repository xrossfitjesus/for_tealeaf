# Regex excer 1 if "lab" exist in a word print the word.

# puts "has word!" if "laboratory" =~ /lab/ or 

#def check(string)
#  if string =~ /lab/
#  puts string 
#  else
#  puts "no cookie"
#  end
#end
#check("laboratory")
#check("experiment")
#check("Pans Labyrinth")
#check("elaborate")
#check("polar bear")

#2 none, since it has to be called with .call 
#3 exception handling, way to handle error in an application, handles it gracefully and
#usually gives yourself an option.

#4 use the .call method
#5 doesnt have the ampersand or & inside the parameter.